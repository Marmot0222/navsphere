# NavSphere

<p align="center">
  <strong>完全本地化的现代导航管理平台</strong>
</p>

## 📖 项目简介

NavSphere 是一个基于 Next.js 15 构建的**完全本地化**导航管理平台，专为个人和团队打造的书签管理和导航门户解决方案。

### 🎯 核心优势

- ✅ **零配置启动** - 无需任何环境变量配置，克隆即用
- ✅ **本地数据存储** - 所有数据存储在本地文件系统，无需外部服务
- ✅ **默认管理员权限** - 无需登录认证，直接访问管理后台
- ✅ **完全离线运行** - 不依赖任何云服务或第三方 API
- ✅ **数据自主可控** - 数据存储在 `navsphere/content/` 目录，完全掌控

<p align="center">
  <img src="doc/n.png" alt="NavSphere 前台" />
</p>
<p align="center">
  <img src="doc/b.png" alt="NavSphere 后台" />
</p>

## ✨ 核心特性

- 🚀 **现代技术栈**: Next.js 15 + React 18 + TypeScript + Tailwind CSS
- 💾 **本地数据存储**: 使用本地文件系统存储数据，无需数据库
- 🔓 **无需认证**: 默认管理员权限，直接访问管理后台
- 📱 **响应式设计**: 移动优先的设计理念，完美适配各种设备
- 🌓 **主题切换**: 内置深色/浅色主题支持
- 🎯 **拖拽排序**: 直观的拖拽界面，轻松管理导航项目
- 🔍 **智能搜索**: 快速定位和访问导航项目
- 📝 **分类管理**: 支持多级分类和子分类
- 🔧 **可视化配置**: 后台界面配置站点信息和导航数据
- ⚡ **快速响应**: 本地数据访问，极速加载

## 🛠️ 技术架构

| 技术栈              | 版本          | 用途                  |
| ------------------- | ------------- | --------------------- |
| **Next.js**         | 15.5.7        | React 全栈框架        |
| **React**           | 18.2.0        | 用户界面库            |
| **TypeScript**      | 5.1.6         | 类型安全的 JavaScript |
| **Tailwind CSS**    | 4.1.12        | 原子化 CSS 框架       |
| **Radix UI**        | Latest        | 无障碍 UI 组件库      |
| **Lucide React**    | 0.462.0       | 现代图标库            |
| **React Hook Form** | 7.53.2        | 表单处理              |
| **Zod**             | 3.22.4        | 数据验证              |

## 🚀 快速开始

### 环境要求

- Node.js 20.0+ 
- pnpm 8.0+ (推荐) 或 npm/yarn

### 安装步骤

1. **克隆项目**
```bash
git clone https://github.com/Marmot0222/navsphere
cd NavSphere
```

2. **安装依赖**
```bash
pnpm install
```

3. **启动开发服务器**
```bash
pnpm dev
```

4. **访问应用**
   
   - 前台页面: [http://localhost:3000](http://localhost:3000)
   - 管理后台: [http://localhost:3000/admin](http://localhost:3000/admin)

就这么简单！无需任何配置，即可开始使用。

## ⚙️ 配置指南

### 数据存储位置

所有数据存储在 `navsphere/content/` 目录下：

```
navsphere/content/
├── navigation.json          # 导航数据
├── navigation-default.json  # 默认导航数据（备份）
├── site.json                # 站点配置
└── resource-metadata.json   # 资源元数据
```

### 后台管理

访问 `/admin` 路径即可进入管理后台，无需登录认证：

- **站点配置**: `/admin/site` - 配置网站标题、Logo、Favicon 等
- **导航管理**: `/admin/navigation` - 管理导航分类和网站链接
- **站点列表**: `/admin/sitelist` - 批量管理所有网站
- **数据统计**: `/admin` - 查看数据统计信息

### 环境变量（可选）

项目无需任何环境变量即可运行。如需自定义配置，可创建 `.env.local` 文件：

```env
# API 配置（可选）
NEXT_PUBLIC_API_URL=http://localhost:3000
```

## 🚀 部署指南

### Docker 部署（推荐）

项目支持 Docker 容器化部署，适合自托管环境。

1. **构建镜像**
```bash
docker build -f docker/Dockerfile -t navsphere:latest .
```

2. **运行容器**
```bash
# 开发环境
docker-compose -f docker/docker-compose.yml up -d

# 生产环境
docker-compose -f docker/docker-compose.prod.yml up -d
```

3. **访问应用**
   - 默认端口: http://localhost:3000

### 常规部署

在任何支持 Node.js 的服务器上：

```bash
# 安装依赖
pnpm install

# 构建项目
pnpm build

# 启动生产服务器
pnpm start
```

### Vercel / Netlify 部署

项目可以部署到 Vercel 或 Netlify，但由于使用了 Node.js 文件系统 API，需要确保：

- 使用 Node.js Runtime（不支持 Edge Runtime）
- 数据文件必须包含在部署包中


## 📊 数据结构

### 数据文件说明

项目使用本地文件系统存储数据，位于 `navsphere/content/` 目录：

| 文件                       | 用途           | 路径                           |
| ---------------------------- | -------------- | -------------------------------- |
| `navigation.json`            | 导航数据       | `navsphere/content/`             |
| `navigation-default.json`    | 默认导航数据   | `navsphere/content/`             |
| `site.json`                  | 站点配置       | `navsphere/content/`             |
| `resource-metadata.json`     | 资源元数据     | `navsphere/content/`             |

### 数据格式示例

<details>
<summary><strong>navigation.json</strong> - 导航数据结构</summary>

```json
{
  "navigationItems": [
    {
      "id": "dev-tools",
      "title": "开发工具",
      "icon": "🛠️",
      "enabled": true,
      "items": [
        {
          "id": "site_001",
          "title": "GitHub",
          "description": "代码托管平台",
          "icon": "https://github.com/favicon.ico",
          "href": "https://github.com",
          "enabled": true
        }
      ],
      "subCategories": [
        {
          "id": "frontend",
          "title": "前端工具",
          "enabled": true,
          "items": []
        }
      ]
    }
  ]
}
```
</details>

<details>
<summary><strong>site.json</strong> - 站点配置结构</summary>

```json
{
  "basic": {
    "title": "NavSphere",
    "description": "现代化导航管理平台",
    "keywords": "导航,书签,管理"
  },
  "appearance": {
    "logo": "/logo.png",
    "favicon": "/favicon.ico", 
    "theme": "system"
  },
  "navigation": {
    "linkTarget": "_blank"
  }
}
```
</details>

## 📝 开发指南

### 可用脚本

```bash
# 开发模式
pnpm dev

# 构建项目
pnpm build

# 启动生产服务器
pnpm start

# 代码检查
pnpm lint

# Docker 部署
pnpm docker:build
pnpm docker:dev
pnpm docker:prod
```

### 项目结构

```
NavSphere/
├── app/                    # Next.js App Router
│   ├── api/               # API 路由
│   ├── admin/             # 管理后台
│   └── page.tsx           # 首页
├── components/            # 共享组件
├── lib/                   # 工具函数
├── navsphere/content/     # 本地数据存储
│   ├── navigation.json
│   └── site.json
├── public/                # 静态资源
└── types/                 # TypeScript 类型
```

## 🐛 故障排除

### 常见问题

<details>
<summary><strong>数据加载失败</strong></summary>

**问题**: 导航数据无法加载

**解决方案**:
1. 检查 `navsphere/content/` 目录是否存在
2. 确认数据文件格式正确（必须是合法的 JSON）
3. 查看浏览器控制台错误信息
4. 检查文件读取权限
</details>

<details>
<summary><strong>构建失败</strong></summary>

**问题**: 项目构建或部署失败

**解决方案**:
1. 检查 Node.js 版本 (需要 20.0+)
2. 清理依赖: `rm -rf node_modules pnpm-lock.yaml && pnpm install`
3. 检查是否有语法错误
4. 查看构建日志详细错误信息
</details>

<details>
<summary><strong>后台修改未同步</strong></summary>

**问题**: 在管理后台修改数据后，前台页面没有更新

**解决方案**:
1. 刷新浏览器页面（Ctrl/Cmd + R）
2. 检查浏览器缓存，尝试强制刷新（Ctrl/Cmd + Shift + R）
3. 检查 `navsphere/content/` 目录下的文件是否已更新
</details>

<details>
<summary><strong>权限问题</strong></summary>

**问题**: 文件无法写入或修改

**解决方案**:
1. 检查 `navsphere/content/` 目录的读写权限
2. 确保运行用户有文件系统访问权限
3. 在 Docker 环境中，检查数据卷映射配置
</details>


## 📜 许可证

本项目基于 [MIT License](LICENSE) 开源协议。

## 🙏 致谢

- [Next.js](https://nextjs.org/) - 强大的 React 框架
- [Tailwind CSS](https://tailwindcss.com/) - 优秀的 CSS 框架
- [Radix UI](https://www.radix-ui.com/) - 无障碍组件库
- 所有为项目做出贡献的开发者

---

<p align="center">
  <strong>⭐ 如果这个项目对你有帮助，请给我们一个 Star！</strong>
</p>
